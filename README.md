```Shell
# 克隆恢复
npm install --save

# 本地拉取更新
git pull

# 本地推送
git add .
git commit -m "<commit-message>"
git push

# 加载子模块
git submodule init
git submodule update

# 更新子模块
git submodule update --remote
```